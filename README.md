Project is dedicated to Redmond SkyPlug and SkyKettle bluetooth devices.

Installation:
- `npm i`
- `npm run tsc` — not sure if it will work like that
- `npm run start`

It's required to make `.sh` scripts executable first
